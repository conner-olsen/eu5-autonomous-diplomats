# Monthly pulse for re-sending recalled diplomats
cmf_monthly_human_country_pulse = {
	on_actions = {
		atd_on_monthly
	}
}

# Pre-populates atd_sync_check_list before flipping the hidden window's gate,
# so iter widgets are instantiated by the time the driver fires.
atd_on_monthly = {
	effect = {
		atd_update_hre_visibility = yes
		atd_gui_sync_begin = yes
		set_variable = atd_pending_action
	}
}
