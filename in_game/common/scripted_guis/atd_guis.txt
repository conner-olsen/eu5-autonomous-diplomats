# Required callback for CMM settings list changes.
atd__improve_relations_on_changed = {
	scope = country
	effect = {
		cmm_apply_list_change = {
			setting = atd__improve_relations
		}
		atd_cmm_handle_callback = yes
	}
}
